services:
  zed_wrapper:
    image: zed-docker-zed_wrapper:latest
    build:
      context: .
      dockerfile: Dockerfile.physical
    container_name: zed_wrapper_container
    runtime: nvidia

    # Network and permissions
    network_mode: host
    ipc: host
    pid: host
    privileged: true
    tty: true
    stdin_open: true
    group_add:
      - dialout

    # Environment
    environment:
      - USER
      - DISPLAY=${DISPLAY:?err}
      - XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR}
      - NVIDIA_DRIVER_CAPABILITIES=all
      - ROS_DOMAIN_ID=0
      # Uncomment if you actually use Zenoh:
      - RMW_IMPLEMENTATION=rmw_zenoh_cpp

    # GPU passthrough
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    # Volumes
    volumes:
      # X11 for RViz / ZED tools
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      # Your local workspace (optional — only if you have custom packages)
      - ../:/home/ws/src
      # Persistent ZED SDK AI models (avoids re-optimizing on every run)
      - zed_resources:/usr/local/zed/resources
      - zed_settings:/usr/local/zed/settings
      # Device passthrough for the camera (USB)
      - /dev:/dev

    # Just launch the ZED 2i wrapper
    command: >
      bash -c "source /opt/ros/jazzy/setup.bash &&
               source /home/zed_deps_ws/install/setup.bash &&
               { ros2 run rmw_zenoh_cpp rmw_zenohd & } &&
               exec ros2 launch zed_display_rviz2 display_zed_cam.launch.py camera_model:=zed2i"

volumes:
  zed_resources:
  zed_settings:
